import { SOLAR_STAR_RECORDS } from './solarSystemRecords'

// Get every star that can be represented by the Horizons-backed solar system dataset.
export const getStars = async () => SOLAR_STAR_RECORDS

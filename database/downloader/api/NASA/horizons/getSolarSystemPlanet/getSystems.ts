import { SOLAR_SYSTEM_RECORDS } from './solarSystemRecords'

// Get every system that can be represented by the Horizons-backed solar system dataset.
export const getSystems = async () => SOLAR_SYSTEM_RECORDS

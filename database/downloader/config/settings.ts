// Settings for use in generating graph database of universe

// # Neighbourhood settings
export const NEIGHBOURHOOD_PROPAGATION_MAX_ITERATIONS = 10 // Maximum number of iterations for label propagation
export const NEIGHBOURHOOD_DISTANCE_PENALTY = 0.5 // Log penalty factor for distance when calculating neighbourhoods

// # Visibility settings
export const VISIBILITY_APPARENT_BRIGHTNESS_THRESHOLD = 6.5 // Approximately what we can see under perfect conditions with the naked eye
